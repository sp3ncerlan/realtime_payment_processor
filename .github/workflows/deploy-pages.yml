name: Deploy Frontend to GitHub Pages

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch: {}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: frontend
        run: |
          npm ci

      - name: Build frontend
        working-directory: frontend
        env:
          VITE_API_BASE_URL: '/api'
        run: |
          npm run build

      - name: Deploy to GitHub Pages (gh-pages branch)
        uses: peaceiris/actions-gh-pages@v4
        with:
          # Use a Personal Access Token stored in the repository secret PAGES_DEPLOY_TOKEN
          # Create a PAT with repo (repo:status, repo_deployment, public_repo) or 'repo' scope
          # and add it to Settings -> Secrets -> Actions as PAGES_DEPLOY_TOKEN.
          # Using a PAT avoids permission 403 for github-actions[bot] in some repos.
          github_token: ${{ secrets.PAGES_DEPLOY_TOKEN }}
          publish_dir: ./frontend/dist
          publish_branch: gh-pages
          # optional: user config
          # publish_user_name: github-actions[bot]
          # publish_user_email: 41898282+github-actions[bot]@users.noreply.github.com
